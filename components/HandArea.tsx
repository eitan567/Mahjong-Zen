import React from 'react';

// This component is no longer used in Mahjong Solitaire but kept to prevent file-not-found errors if imported elsewhere.
export const HandArea: React.FC<any> = () => {
  return null;
};